import "./dine.css";

export const metadata = {
  title: "Dine — COGNOS",
};

export default function DineLayout({ children }: { children: React.ReactNode }) {
  return (
    <>
      {children}
    </>
  );
}
